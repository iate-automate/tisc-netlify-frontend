# Portal secrets — constants: config/settings.json
# AIRTABLE_PASSWORD and OPENAI_API_KEY are NOT used here — they belong to
# the Functions backend (tisc-firebase-functions), not this frontend.

AIRTABLE_API_KEY=
FIREBASE_PRIVATE_KEY=
